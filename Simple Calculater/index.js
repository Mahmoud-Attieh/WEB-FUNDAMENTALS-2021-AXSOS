function addNumbers(x, y) {
    return x + y;
}
var sum = addNumbers(5, 10);
console.log(sum); 

function subNumbers(x, y) {
    let r= x - y;
    console.log (x-y)
}
var sub = subNumbers(8, 10);
//console.log(sub); 

function mulNumbers(x, y) {
    return x * y;
}
var mul = mulNumbers(5, 10);
console.log(mul); 

function divNumbers(x, y) {
    return x % y;
}
var div = divNumbers(5, 10);
console.log(div); 